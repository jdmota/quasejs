import { SerializationDB } from "../../util/serialization";

export const serializationDB = new SerializationDB();

import { SerializationRegistry } from "../../util/serialization-2";

export const serializationRegistry = new SerializationRegistry();
